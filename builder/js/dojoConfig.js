var dojoConfig = {
  paths: {
    extras: location.pathname.replace(/\/[^/]+$/, "") + "/js/modules",
    lists: location.pathname.replace(/\/[^/]+$/, "") + "/js/lists",
    templates: location.pathname.replace(/\/[^/]+$/, "") + "/js/templates"
  }
};
